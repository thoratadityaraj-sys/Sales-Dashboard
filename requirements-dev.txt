# Only needed to RE-RUN build_artifacts.py locally when the underlying data changes.
# The deployed Streamlit app itself only needs requirements.txt (streamlit, pandas, plotly) -
# it just reads the CSVs that build_artifacts.py produces.
prophet>=1.1
scikit-learn>=1.3
numpy>=1.24
